{"parameters":[{"name":"limit","in":"query","required":false,"schema":{"type":"integer","maximum":1000,"minimum":1,"description":"Max DLQ entries to return. Default 100, hard cap 1000. Only entries that have NOT been replayed are returned (pass ?include_replayed=true for the full audit history).","default":100,"title":"Limit"},"description":"Max DLQ entries to return. Default 100, hard cap 1000. Only entries that have NOT been replayed are returned (pass ?include_replayed=true for the full audit history)."},{"name":"include_replayed","in":"query","required":false,"schema":{"type":"boolean","description":"When True, also return entries that have been replayed (audit history). Default False (only pending).","default":false,"title":"Include Replayed"},"description":"When True, also return entries that have been replayed (audit history). Default False (only pending)."},{"name":"authorization","in":"header","required":false,"schema":{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Authorization"}}]}