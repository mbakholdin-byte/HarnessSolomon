{
  "hook_id": "user.builtin.secret_detect",
  "event": "PreToolUse",
  "transport": "builtin",
  "matcher": "tool_name=write_file,tool_name=edit_file,tool_name=bash",
  "enabled": true,
  "priority": 90,
  "description": "Phase 4.10 Task B: PreToolUse regex scan for AWS / GitHub / OpenAI keys, PEM private keys, JWTs, and password= literals in tool arguments. Fail-closed: a match produces decision='block' with a human-readable reason naming the matched pattern family. The matcher restricts to tools that can persist / echo secrets (write_file, edit_file, bash)."
}
