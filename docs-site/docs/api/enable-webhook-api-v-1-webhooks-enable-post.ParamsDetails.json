{"parameters":[{"name":"url","in":"query","required":true,"schema":{"type":"string","description":"The outbound webhook URL to re-enable. Outbound URLs contain slashes and colons (``https://host/path``) which can't appear raw in a path segment, so the URL is passed as a query parameter (``?url=<value>``). Percent-encoding is optional — Starlette decodes query params once.","title":"Url"},"description":"The outbound webhook URL to re-enable. Outbound URLs contain slashes and colons (``https://host/path``) which can't appear raw in a path segment, so the URL is passed as a query parameter (``?url=<value>``). Percent-encoding is optional — Starlette decodes query params once."},{"name":"authorization","in":"header","required":false,"schema":{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Authorization"}}]}